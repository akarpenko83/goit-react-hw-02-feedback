// import React from 'react';

export default function Section({ children }) {
    return (
        <section title="">
            <h2>Please leave feedback</h2>
            {children}
        </section>
    );
}
